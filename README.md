# 🛒 ISell Ecommerce API

Современный и мощный RESTful API для e-commerce платформы. Построен на Django REST Framework с полным функционалом управления продуктами, заказами и пользователями.

## 📋 О проекте

ISell Ecommerce - это backend API сервис для мобильных и веб-приложений. Проект предоставляет следующие основные функции:

- 📱 **Аутентификация по номеру телефона** (с SMS кодом)
- 🛍️ **Управление продуктами** (категории, изображения, характеристики)
- 🛒 **Система заказов** (график платежей, тарифы)
- 💳 **Расчеты платежей** (ежемесячные платежи, графики)
- 📊 **Swagger API документация**

## 🚀 Технологии

### Backend Framework
- **Django** 5.2.7
- **Django REST Framework** 3.16.1
- **djangorestframework-simplejwt** 5.5.1

### База данных
- **PostgreSQL** (psycopg2 2.9.11)

### Дополнительные библиотеки
- **drf-yasg** - Swagger/OpenAPI документация
- **django-cors-headers** - Настройки CORS
- **django-filter** - Фильтрация API
- **django-import-export** - Импорт/экспорт данных
- **Pillow** - Обработка изображений
- **requests** - HTTP запросы

### Сервисы
- **Eskiz SMS** - Отправка SMS сообщений

## 📦 Установка

### Требования
- Python 3.8+
- PostgreSQL 12+
- pip

### Шаги

1. **Клонирование проекта**
```bash
git clone <repository-url>
cd ISell_Ecommerce
```

2. **Создание и активация виртуального окружения**
```bash
# Windows
python -m venv env
env\Scripts\activate

# Linux/Mac
python3 -m venv env
source env/bin/activate
```

3. **Установка необходимых пакетов**
```bash
pip install -r requirements.txt
```

4. **Создание базы данных**
```sql
CREATE DATABASE isell_ecommerce;
```

5. **Настройка переменных окружения**

Создайте файл `.env` и заполните следующие переменные:

```env
# Database
DB_NAME=isell_ecommerce
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# Eskiz SMS
ESKIZ_EMAIL=your_eskiz_email
ESKIZ_PASSWORD=your_eskiz_password

# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
```

6. **Применение миграций к базе данных**
```bash
python manage.py migrate
```

7. **Создание суперпользователя** (опционально)
```bash
python manage.py createsuperuser
```

8. **Запуск сервера**
```bash
python manage.py runserver
```

Сервер будет доступен по адресу `http://127.0.0.1:8000/`.

## 🔧 Настройка

### База данных

Настройки PostgreSQL в файле `config/settings.py`:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('DB_NAME'),
        'USER': os.getenv('DB_USER'),
        'PASSWORD': os.getenv('DB_PASSWORD'),
        'HOST': os.getenv('DB_HOST'),
        'PORT': os.getenv('DB_PORT'),
    }
}
```

### JWT Аутентификация

JWT токены настраиваются в файле `config/libraries/jwt.py`. Срок действия токена и другие параметры изменяются там.

### Настройки CORS

Настройки CORS управляются в файле `config/libraries/cors.py`.

## 📚 API Endpoints

### 🔐 Аутентификация (`/api/v1/accounts/`)

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/login/` | POST | Вход по номеру телефона (отправляет SMS код) |
| `/verify/` | POST | Подтверждение SMS кода и получение JWT токена |
| `/resend/` | POST | Повторная отправка SMS кода |

**Пример: Login**
```json
POST /api/v1/accounts/login/
{
    "phone_number": "+998901234567"
}
```

### 🛍️ Продукты (`/api/v1/products/`)

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/` | GET | Список всех продуктов |
| `/<product_id>/` | GET | Детали продукта |
| `/<product_id>/filter/` | GET | Отфильтрованные детали продукта |
| `/<product_id>/calculate/` | POST | Расчет ежемесячного платежа |
| `/categories/` | GET | Список категорий |
| `/calculate-schedule/` | POST | Расчет графика платежей |
| `/import-categories/` | POST | Импорт категорий |
| `/import-products/` | POST | Импорт продуктов |
| `/import-characteristics/` | POST | Импорт характеристик |
| `/import-advanced-payment/` | POST | Импорт оценки авансового платежа |
| `/import-images/` | POST | Импорт изображений продуктов |

### 🛒 Заказы (`/api/v1/order/`)

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/create/` | POST | Создание нового заказа |
| `/tariffs/` | GET | Список тарифов |
| `/company-addresses/` | GET | Адреса компаний |
| `/update-address/` | PUT | Обновление адреса заказа |
| `/import-tariffs/` | POST | Импорт тарифов |

## 📖 API Документация

Проект оснащен Swagger/OpenAPI документацией:

- **Swagger UI**: `http://127.0.0.1:8000/swagger/`
- **ReDoc**: `http://127.0.0.1:8000/redoc/`
- **JSON Schema**: `http://127.0.0.1:8000/swagger.json`

В API документации показаны все endpoints, параметры и детали ответов.

## 🗄️ Структура базы данных

### Accounts (Пользователи)
- `CustomUser` - Модель пользователя (на основе номера телефона)
- `SmsCode` - SMS коды
- `EskizToken` - Токены Eskiz API

### Products (Продукты)
- `Categories` - Категории
- `Products` - Продукты
- `ProductCategory` - Оценка авансового платежа
- `ProductIDs` - Вариации продуктов
- `ProductImages` - Изображения продуктов
- `ProductDetails` - Детали продуктов (цвет, память, SIM)
- `ProductProperties` - Свойства продуктов
- `ProductCharacteristics` - Характеристики продуктов

### Order (Заказы)
- `Tariffs` - Тарифы платежей
- `OrderCaluculationMode` - Режимы расчета
- `Orders` - Заказы
- `OrderItems` - Товары заказа
- `OrderPaymentSchedule` - График платежей
- `CompanyAddress` - Адреса компаний

## 🔑 Аутентификация

API использует JWT (JSON Web Token) аутентификацию:

1. **Login**: Отправьте номер телефона, получите SMS код
2. **Verify**: Подтвердите SMS код, получите JWT токен
3. **API запросы**: Отправляйте токен в каждом запросе:

```
Authorization: Bearer <your_jwt_token>
```

## 🎯 Основные функции

- ✅ **Аутентификация по номеру телефона** - с SMS кодом
- ✅ **Управление продуктами** - Полные CRUD операции
- ✅ **Каскадная фильтрация** - Фильтрация по цвету, памяти, SIM карте
- ✅ **Расчеты платежей** - Ежемесячные платежи и графики
- ✅ **Система заказов** - Полное управление заказами
- ✅ **Загрузка изображений** - Загрузка изображений продуктов
- ✅ **Import/Export** - Импорт/экспорт данных
- ✅ **API документация** - С Swagger/ReDoc
- ✅ **Настройки CORS** - Для cross-origin запросов

## 🛠️ Разработка

### Создание миграций
```bash
python manage.py makemigrations
python manage.py migrate
```

### Сбор статических файлов
```bash
python manage.py collectstatic
```

### Запуск тестов
```bash
python manage.py test
```

## 📁 Структура проекта

```
ISell_Ecommerce/
├── apps/
│   └── v1/
│       ├── accounts/          # Пользователи и аутентификация
│       ├── products/          # Продукты
│       └── order/             # Заказы
├── config/                    # Основные настройки
│   ├── libraries/            # Настройки библиотек
│   └── middleware/           # Middleware
├── env/                      # Виртуальное окружение
├── static/                   # Статические файлы
├── manage.py                 # Файл управления Django
└── requirements.txt          # Список пакетов
```

## 🔒 Безопасность

- JWT токен аутентификация
- Хеширование паролей
- Настройки CORS
- Защита от CSRF
- Защита от SQL инъекций (Django ORM)

## 📝 Примечания

- В production среде установите `DEBUG=False`
- Храните `SECRET_KEY` в безопасности
- Защитите пароль базы данных
- Введите правильные настройки для SMS сервиса (Eskiz)

## 👥 Авторы

Команда ISell Ecommerce

## 📄 Лицензия

Этот проект разработан как частный проект.

---

**Контакты**: Для вопросов или предложений обращайтесь к авторам проекта.

**Помощь**: Для просмотра API документации перейдите на endpoint `/swagger/`.
